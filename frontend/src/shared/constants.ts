/** Target intensity (2025) gCO₂e/MJ — matches backend */
export const TARGET_INTENSITY = 89.3368;

/** Default ship for banking demo (surplus under target in seed data). */
export const DEFAULT_SHIP_ID = "SHIP-002";
